# AIMM website (GitHub Pages)

Static project website with a right-hand navigation.

## Publisher view counts
Frontiers article view counts are stored in `assets/frontiers_metrics.json` and updated weekly via GitHub Actions.

Enable Actions write permissions:
Settings → Actions → General → Workflow permissions → Read and write.
